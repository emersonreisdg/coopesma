Instalação
==========

Para instalar as dependências do projeto, execute:

.. code-block:: bash

    pip install -r requirements.txt

Certifique-se também de ter o OpenCV instalado:

.. code-block:: bash

    pip install opencv-python
